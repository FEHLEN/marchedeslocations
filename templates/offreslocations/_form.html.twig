<div class="container">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="bouton-annonce">{{ button_label|default('Enregistrer') }}</button>
    <a  type="button" href="{{ path('app_accueil') }}" class="bouton-annonce">Annuler</a>
{{ form_end(form) }}
</div>